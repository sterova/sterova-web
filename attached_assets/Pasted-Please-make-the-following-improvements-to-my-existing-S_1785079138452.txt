Please make the following improvements to my existing Sterova website. This is not a rebuild; only apply these alterations to the current site.

### 1) Homepage visuals

Enhance the homepage with smooth, modern background animations that feel premium and engaging without hurting performance. Use subtle effects such as animated gradients, floating elements, glowing particles, or soft motion to add depth and visual appeal.

Also improve the color system for both **Light Mode** and **Dark Mode**. The palettes should feel modern, elegant, and consistent across the website, with strong contrast, readability, and accessibility. Keep the overall look polished and premium.

### 2) Navbar services hover

The Services link in the top navbar needs a better hover style. Make it more unique and visually refined. Add a smooth background-style animation, like moving lines or subtle motion in the background. Keep all animations smooth, tasteful, and responsive.

### 3) Human-like design and content

I do not want the website to look or read like it was generated by AI. Make the styling and content feel more human, natural, and realistic. The current style is acceptable, but the colors and visual polish need improvement.

### 4) Reviews section

The review write section should always remain visible, and submitted reviews should appear in the review cards section.

* Remove the default reviews from the live UI.
* Load customer-written reviews from the database and display them dynamically.
* Keep the default sample reviews only inside `/data/constants.jsx` as commented-out fallback content so I can modify them later.

### 5) Services content

Move all services into `/data/constants.jsx` so I can edit them easily.

Current services I want to keep:

* Web Development
* Custom Software Development
* Mobile App Development

Remove these default services:

* Cybersecurity
* UI/UX Design
* Cloud/DevOps
* AI & Automation

Also make the services dynamic:

* If I add a new service in constants, it should show automatically.
* The top navbar should also update dynamically based on the services I add.
* Apply the same dynamic behavior to all content sections, not just services.

### 6) Blog error

Fix the blog page error caused by Supabase:

```text
if (error) {
  console.error({
  code: error.code,
  message: error.message,
  details: error.details,
  hint: error.hint,
});
  return [];
}
```

Make the blog page handle errors gracefully without breaking the UI. Add proper loading, error, and empty states.

### 7) Secure admin system

I want a highly secure, private admin login that is only for me.

Requirements:

* The admin login page must not appear in Google search.
* Add proper `noindex` / `nofollow` handling for admin routes.
* Use strong validation.
* Add rate limiting.
* Add security protections for login and admin access.
* Keep the admin system fully private.

Admin should be able to:

* Add projects
* Delete projects
* Add blogs
* Delete blogs
* Manage website content
* Update editable content across the site

### 8) Move editable content to database

Move all editable website content into Supabase so I can manage it from the admin panel later. Generate the SQL needed for Supabase to support this structure.

The database should include tables for:

* services
* portfolio projects
* testimonials
* reviews
* blog posts
* blog categories
* FAQ items
* site content
* navigation items
* admins
* audit logs

### 9) Dynamic content behavior

The website should work dynamically everywhere:

* New constants should show automatically.
* New services should appear automatically.
* Navbar content should reflect the current data.
* Reviews, blog posts, and editable sections should all be data-driven.
* The same approach should apply across the whole website.

### 10) Final goal

Keep the site smooth, responsive, premium, and easy to maintain, while making the design feel more natural and the content fully dynamic.
