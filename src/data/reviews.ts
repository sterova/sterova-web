import type { Review } from "@/types";

// Reviews will be populated from the backend once connected.
// This array is intentionally empty — no placeholder data.
export const STATIC_REVIEWS: Review[] = [];
